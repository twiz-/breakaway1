<% if can_favorite?(profile) %>
  <% unless current_user.in_favorites?(profile) %>
    <%= link_to "Add fav", new_friendship_path(friend_id: profile), class: "btn btn-mini btn-warning" %>
  <% else %>
    In favorites
  <%end%>
<% end %>
